# CCG-MCP 配置文件示例（可选）
# 复制此文件到 ~/.ccg-mcp/config.toml 以自定义默认模型
# 如不创建配置文件，将使用下方的默认值

# Coder 配置
[coder]
model = "gpt-5.2-codex"  # 默认 gpt-5.2-codex

# Codex 配置
[codex]
model = "gpt-5.2-codex"  # 默认 gpt-5.2-codex

# Gemini 配置
[gemini]
model = "gemini-3-pro-preview"  # 默认 gemini-3-pro-preview
